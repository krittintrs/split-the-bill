---
target: all shipped surfaces (src/app)
total_score: 28
p0_count: 0
p1_count: 2
timestamp: 2026-07-19T13-27-34Z
slug: src-app
---
# Critique — src/app (all shipped surfaces), 2026-07-19

Method: dual-agent (A: design review w/ browser · B: detect.mjs CLI)
Post-PR-22 state (issue #19 UX fixes merged, v0.2.0 + PR 22).

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Autosave success is silent; only errors speak |
| 2 | Match System / Real World | 2 | "Checksum" jargon; locked bill labeled ฉบับร่าง (wrong) |
| 3 | User Control and Freedom | 3 | Good confirms; deletes permanent, no undo |
| 4 | Consistency and Standards | 3 | เปิดแล้ว success pill lacks icon in 3 places (breaks own DESIGN.md rule) |
| 5 | Error Prevention | 3 | Publish gated at 0 items; money normalizes on blur |
| 6 | Recognition Rather Than Recall | 3 | Peer desktop tick cells are blank/invisible |
| 7 | Flexibility and Efficiency | 3 | Real links, recent-peer chips; no bulk actions |
| 8 | Aesthetic and Minimalist Design | 3 | Checksum printed twice; รวมต่อคน duplicates it |
| 9 | Error Recovery | 3 | Inline save-error banner + retry = best-in-class |
| 10 | Help and Documentation | 2 | Zero peer onboarding; no "which column is me" |
| **Total** | | **28/40** | **Good — solid foundation, address weak areas** |

## Anti-Patterns Verdict

Does NOT read as AI-made. LLM review: no gradient text, glassmorphism, side-stripes, eyebrows, hero-metrics; real Thai voice + committed cyan + checksum-as-hero. Mild tells: same-y rounded-xl card stack by 5th editor section; bilingual seams (Sign in with Google / Publish / Checksum / Service charge).
Deterministic scan: **clean, exit 0, zero findings** across 19 files / 45 rules. Detector and LLM agree. Browser overlay skipped (shared browser reserved for Assessment A).

## Priority Issues

- **[P1] Peer tick/paid cells invisible on desktop matrix** — idle = `bg-surface-tint text-transparent` (PeerBill.tsx:280,313): blank pale squares, indistinguishable from disabled. The peer's core action is undiscoverable. Fix: faint glyph/outline for idle, distinct disabled treatment. → /impeccable polish
- **[P1] ConfirmDialog renders top-left, not centered** (ConfirmDialog.tsx:44) — `open:flex` kills native `<dialog>` centering; scariest moment looks like a rendering bug. Fix: restore margin:auto centering. → /impeccable polish
- **[P2] Locked bill shows ฉบับร่าง badge** (BillListItem.tsx:26-34, only branches open-vs-else) and delete button silently vanishes. Fix: 3-state badge w/ lock icon. → /impeccable polish
- **[P2] Checksum = jargon + false-red empty state** — fresh bill greets organizer in danger red (`ยังไม่ได้กรอกยอดใบเสร็จ` styled as mismatch). Fix: Thai label (ตรวจยอด), neutral ink-muted until a receipt total exists. → /impeccable clarify
- **[P2] No loading feedback on editor→peer nav or /b/[id], /bills/[id] routes** — useLinkStatus only covers dashboard rows; no loading.tsx anywhere. → /impeccable polish
- **[P3] Open-editor header: 4 co-equal buttons, delete loudest, share quiet.** Promote คัดลอกลิงก์ to primary, demote ลบบิล. → /impeccable layout
- **[P3] Mobile pills wrap 2 lines at 390 (ฉบับ/ร่าง); success pills missing ✓ icon; dashboard rows jagged right edge when ✕ absent.** → /impeccable polish

## Cognitive Load

3 failures: working memory (peer must re-find own name every row — no "this is you" binding), chunking (6-control item row), single focus (post-publish header). Worst decision points: peer-desktop blank squares; post-publish "which button shares?"; 6-field menu row.

## Emotional Journey

Peaks land: checksum flip to green, คัดลอกลิงก์แล้ว ✓, จ่ายแล้ว. Valleys: red greeting on fresh bill; top-left delete dialog at max-anxiety moment; peer-desktop confusion. Peak-end: organizer's finale (share link) is unceremonious; peer gets no "คุณต้องจ่าย ฿X" closure.

## Persona Red Flags

- **Jordan (first-time peer, Slack link, desktop)** — worst hit: blank matrix squares, no toggle affordance, no identity cue; can tick as wrong person.
- **Casey (one-handed mobile)** — top-left dialog unreachable; wrapping pills read broken.
- **Alex (organizer at the table)** — silent autosave (no confidence), 6-field row slows entry, post-publish ambiguity.

## Minor Observations

Checksum shown twice on editor; รวมต่อคน duplicates checksum; editor idle matrix dot barely visible (ink-muted/50 on surface-tint); peer's own total never elevated ("numbers are the product" unmet); ConfirmDialog mounted per dashboard row (3 in DOM); hover color snaps (transition-transform omits color).

## Questions to Consider

1. Peer link resolves to a shared everyone-grid — why not to *a person* with "you owe ฿X" up top?
2. What would the organizer actually call "does this add up?" — why isn't that the label instead of "Checksum"?
3. เปิดแล้ว pill breaks the green-needs-icon rule in 3 places — three bugs, or a rule too strict?
